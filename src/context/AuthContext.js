import React, { createContext, useState, useEffect } from 'react';

export const AuthContext = createContext();

const AuthProvider = ({ children }) => {
  const [isAuthenticated, setIsAuthenticated] = useState(false);

  useEffect(() => {
    const token = localStorage.getItem('token');
    if (token) {
      setIsAuthenticated(true);
    }
  }, []);

  return (
    <AuthContext.Provider value={{ isAuthenticated, setIsAuthenticated }}>
      {children}
    </AuthContext.Provider>
  );
};

export default AuthProvider;

/* 
AuthProvider is a context provider that manages authentication state using isAuthenticated and setIsAuthenticated.
It initializes the authentication state by checking for a token in localStorage with useEffect.
It initializes the authentication state by checking for a token in localStorage with useEffect.
*/